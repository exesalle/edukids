import React from 'react';

type Props = {
  title: string,
  isOpen: boolean,
  onOk: (val: string) => void;
  onCancel: (val: string) => void;
}

const GeneralModal = () => {
  return (
    <div>

    </div>
  );
};

export default GeneralModal;